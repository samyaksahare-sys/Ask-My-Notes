# Web layer
fastapi
uvicorn[standard]

# PDF parsing + chunking
pypdf
langchain-text-splitters

# Vector store (embeddings come from the Gemini API)
chromadb

# LLM + config
google-genai
python-dotenv
